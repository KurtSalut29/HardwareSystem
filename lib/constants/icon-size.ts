// Shared icon size scale. Use instead of ad-hoc pixel values so icons
// stay visually consistent across the app.
//   xs — table-row actions, inline badge icons
//   sm — secondary inline icons, form-field icons
//   md — modal close buttons, standard button/nav icons
//   lg — page-header / stat-card icons
//   xl — large empty-state / hero icons
export const ICON_SIZE = {
  xs: 12,
  sm: 14,
  md: 16,
  lg: 20,
  xl: 24,
} as const;
